# ST1005: Docker-compatible error strings are capitalized on purpose
#         ("No such container: x" is what the docker CLI matches on).
# SA1019: grpc.CallCustomCodec (grpcbridge.go) — ForceCodec needs a codec
#         with a different interface; tracked separately.
checks = ["inherit", "-ST1005", "-SA1019"]
